#!/bin/bash
set -euo pipefail

port="${OPENCODE_SERVER_PORT:-4096}"
code="$(curl -sS -o /dev/null -w '%{http_code}' --max-time 15 "http://127.0.0.1:${port}/" || true)"
case "$code" in
  200|301|302|401|403|404) exit 0 ;;
  *) echo "opencode unhealthy: http ${code:-000}" >&2; exit 1 ;;
esac
